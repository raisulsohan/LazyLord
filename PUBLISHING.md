# LazyLord প্রকাশ করা

ব্যবহারকারীর হাতে LazyLord দুই টুকরো হয়ে যায়, দুই পথে:

| টুকরো | কী হয়ে যায় | কোথা দিয়ে যায় |
| --- | --- | --- |
| Photoshop, Illustrator, After Effects প্যানেল | সাইন করা `.zxp`, একটা জিপের ভেতরে | আপনার নিজের লিংক (GitHub Releases), চাইলে পরে Adobe Exchange |
| Figma প্লাগিন | Figma-র নিজের প্যাকেজ | Figma Community |

Adobe-র দিকটা স্ক্রিপ্টেই হয়ে যায়। Figma-র দিকটা Figma-র অ্যাপ থেকে হাতে করতে হয় —
Figma স্ক্রিপ্ট দিয়ে প্লাগিন প্রকাশ করতে দেয় না।

---

## ১. Adobe প্যানেল — `.zxp` বানানো

### একবারের প্রস্তুতি

```bash
npm run release:cert
```

এটা দুটো কাজ করে:

1. Adobe-র নিজের সাইনিং টুল `ZXPSignCmd` নামিয়ে `tools/vendor/`-এ রাখে
   (Adobe-CEP/CEP-Resources রিপো থেকে, ৪.৫ MB)।
2. একটা self-signed সার্টিফিকেট বানায় আর তার পাসওয়ার্ড লিখে রাখে, মেয়াদ ১০ বছর।
   দুটোই যায় রিপোর **বাইরে**, `D:\LazyLord Release\Signing key (do not share)\`
   ফোল্ডারে (`LAZYLORD_RELEASE_DIR` দিয়ে জায়গা বদলানো যায়)।

> **⚠ `Signing key (do not share)` ফোল্ডারটা ব্যাকআপ রাখুন — পেনড্রাইভে, বা যেখানে নিরাপদ।**
> এই সার্টিফিকেট দিয়েই পরের সব রিলিজ সাইন করতে হবে। হারিয়ে গেলে নতুন
> সার্টিফিকেট বানাতে হবে, আর তখন ব্যবহারকারীর ইনস্টল করা পুরোনো LazyLord আর
> আপডেট হবে না — আগে ওটা মুছে তারপর নতুন করে ইনস্টল করতে হবে।
>
> রিপোর ভেতরে কিছুই লেখা হয় না, তাই ভুল করে কমিট হওয়ার পথও নেই। এই ফাইল বা পাসওয়ার্ড
> কখনো GitHub-এ তুলবেন না — যার কাছে এটা আছে সে আপনার নামে যেকোনো প্যানেল সাইন
> করতে পারবে।

### প্রতিবার রিলিজ

```bash
npm run release
```

যা করে: বিল্ড → প্যানেলের ফাইল আলাদা করে সাজানো → সাইন ও টাইমস্ট্যাম্প →
যাচাই → ব্যবহারকারীর ফোল্ডার সাজানো → জিপ।

শেষে পাবেন **একটাই ফাইল** — `D:\LazyLord Release\LazyLord-<version>.zip`। মাঝপথের
সব কিছু (staging, না-জিপ-করা ফোল্ডার) সিস্টেমের temp-এ হয় আর শেষে মুছে যায়, তাই
রিলিজ ফোল্ডারে জিপ ছাড়া আর কিছু জমে না। জিপের ভেতরে:

```
LazyLord-1.0.0.zxp            প্যানেল, সাইন করা
Install LazyLord.bat          Windows — ডাবল ক্লিক
Install LazyLord (macOS)      macOS — ডাবল ক্লিক
Uninstall LazyLord.bat
Fix a blank panel.bat         প্যানেল ফাঁকা এলে
Read me first.txt             ব্যবহারকারীর নির্দেশনা
Figma plugin\                 manifest.json + dist (Community-তে ওঠার আগ পর্যন্ত)
```

ব্যবহারকারীর কিছু লাগে না — Node নেই, ব্রিজের কালো উইন্ডো নেই, ডিবাগ মোড নেই,
আলাদা কোনো এক্সটেনশন ম্যানেজারও লাগে না। জিপ খুলে `Install LazyLord.bat` চালালেই
প্যানেল `%APPDATA%\Adobe\CEP\extensions\com.lazylord.panel`-এ বসে যায়।

### ভার্সন বদলানো

শুধু `package.json`-এর `version` বদলান। `npm run release` নিজেই তিন জায়গায়
(`CSXS/manifest.xml`, `jsx/lazylord.jsx`, `js/main.js`) সেটা বসিয়ে দেয়। Adobe এই manifest-এর
ভার্সন দেখেই ঠিক করে ইনস্টলটা আপডেট না নতুন, তাই এটা পুরোনো থেকে গেলে আপডেট
"আগেই ইনস্টল করা আছে" বলে বসে থাকে।

### অন্যান্য কমান্ড

```bash
node tools/package-zxp.mjs --skip-build   # বিল্ড ছাড়া, শুধু প্যাকেজ
node tools/get-zxpsigncmd.mjs --force     # সাইনিং টুল আবার নামাও
```

### দুটো সীমা জেনে রাখুন

- **`OS Trusted: false`** — যাচাইয়ের আউটপুটে এটা দেখাবে, এটাই স্বাভাবিক।
  self-signed সার্টিফিকেট Windows বা macOS-এর বিশ্বস্ত তালিকায় থাকে না। Adobe
  এতে প্যানেল লোড করতে আপত্তি করে না। টাকা দিয়ে কেনা code-signing সার্টিফিকেট
  (Certum-এর open source সংস্করণ সস্তা) থাকলে `lazylord.p12`-এর
  জায়গায় সেটা রেখে দিলেই চলবে।
- **যে সিস্টেমের জন্য, সেই সিস্টেমেই সাইন করা ভালো।** Adobe-র নভেম্বর ২০২৪-এর
  নোট বলছে এক প্ল্যাটফর্মে সাইন করা প্যাকেজ অন্যটায় যাচাইয়ে আটকে যেতে পারে।
  Windows-এ বানানো জিপ ম্যাকেও কাজ করার কথা (আমাদের প্যানেলে symlink নেই, ওটাই
  ছিল আসল কারণ), কিন্তু ম্যাক ব্যবহারকারী পেলে একবার ম্যাকে `npm run release`
  চালিয়ে আলাদা জিপ দেওয়াই নিরাপদ।

---

## ২. ব্যবহারকারীর কাছে পৌঁছে দেওয়া

### ক. নিজের লিংকে — আজই করা যায়

GitHub-এ রিপো তোলার পর:

1. রিপোর পাতায় **Releases → Draft a new release**।
2. ট্যাগ `v1.0.0`, শিরোনাম `LazyLord 1.0.0`।
3. `D:\LazyLord Release\LazyLord-1.0.0.zip` ফাইলটা টেনে ছেড়ে দিন।
4. বিবরণে ছোট করে লিখুন: কী কী নতুন, কোন কোন অ্যাপ লাগে, আর "জিপ খুলে
   Install LazyLord.bat চালান"।
5. Publish।

ব্যবহারকারী একটা লিংক পায়, নামায়, ডাবল ক্লিক করে। এটাই সবচেয়ে সহজ পথ, আর
এটা আপনার হাতেই থাকে — কারও রিভিউর অপেক্ষা নেই।

### খ. Adobe Exchange — ঐচ্ছিক, পরে

Adobe-র নিজের মার্কেটপ্লেসে উঠলে ব্যবহারকারী Creative Cloud অ্যাপ থেকেই
ইনস্টল করতে পারে। কিন্তু:

- সাইন আপ করতে হবে <https://developer.adobe.com/developer-distribution/> —
  ব্যক্তিগত ডেভেলপার অ্যাকাউন্ট নিজে থেকেই তৈরি হয়ে যায়, খরচ নেই।
- একটা লিস্টিং বানিয়ে `.zxp` আপলোড করে রিভিউয়ে পাঠাতে হয়।
- Adobe এখন UXP-কে সামনে রাখছে, আর তাদের নতুন পোর্টালের নথিপত্র মূলত UXP নিয়েই।
  CEP প্যানেল এখনো Exchange-এ আছে ও চলে, কিন্তু নতুন CEP জমা নেওয়ার নিয়ম
  বদলে থাকতে পারে — পোর্টালে ঢুকে দেখতে হবে।

পরামর্শ: আগে (ক) দিয়ে ছাড়ুন, কয়েকজন ব্যবহারকারীর হাতে চলুক, তারপর Exchange।

---

## ৩. Figma প্লাগিন — Community-তে প্রকাশ

### ধাপ ১: আসল plugin id নিন (এটা না করলে প্রকাশ হবে না)

এখন `packages/figma-plugin/manifest.json`-এ আছে `"id": "lazylord-figma-transfer"`।
এটা হাতে লেখা, নিজে চালানোর জন্য ঠিক আছে, কিন্তু **Figma-র দেওয়া id ছাড়া
প্রকাশ করা যায় না** — id Figma নিজে দেয়।

1. Figma ডেস্কটপ অ্যাপে **Plugins → Development → New plugin…**
2. নাম `LazyLord`, ধরন **Figma + FigJam** (বা শুধু Figma), তারপর যেকোনো
   টেমপ্লেট বেছে একটা অস্থায়ী ফোল্ডারে সেভ করুন।
3. ওই ফোল্ডারের `manifest.json` খুলে `"id"`-র নম্বরটা কপি করুন (বড় একটা সংখ্যা)।
4. সেটা আমাদের `packages/figma-plugin/manifest.json`-এ `"id"`-র জায়গায় বসান।
5. Figma-তে পুরোনো ইমপোর্ট করা LazyLord সরিয়ে আমাদের manifest আবার ইমপোর্ট করুন।

### ধাপ ২: ছবি দুটো তৈরি করুন

Figma Community-তে লাগে:

- **আইকন** — ১২৮×১২৮ PNG
- **কভার আর্ট** — ১৯২০×৯৬০ PNG

দুটোর নকশা রেডি আছে: `assets/icon.svg` আর `assets/banner.svg`। Figma-তে ফাইল
দুটো টেনে এনে ফ্রেম বেছে **Export → PNG → 1x**
করলেই ঠিক মাপে বেরোবে। পছন্দ না হলে নিজের মতো বদলে নিন।

### ধাপ ৩: প্রকাশ

1. Figma ডেস্কটপ অ্যাপে **Plugins → Development → LazyLord → Publish…**
   (অথবা Manage plugins in development থেকে)।
2. যা চাইবে:
   - **Name** — LazyLord
   - **Tagline** — এক লাইন। যেমন: "Send vectors, live text and images to
     Photoshop, Illustrator and After Effects."
   - **Description** — কী করে, কী কী যায়, কী লাগে। এখানে **অবশ্যই লিখবেন** যে
     Adobe প্যানেলটা আলাদা করে ইনস্টল করতে হয়, আর ডেস্কটপ Figma লাগে। সঙ্গে
     ইনস্টলের লিংক (GitHub Releases) দিন — Figma চায় লিস্টিং থেকে সেটআপের
     নির্দেশনা পাওয়া যাক।
   - **Tags** — যেমন: illustrator, photoshop, after effects, export, vector,
     handoff, motion
   - **Support contact** — একটা ইমেইল বা GitHub issues-এর লিংক। এটা বাধ্যতামূলক।
   - আইকন আর কভার আর্ট।
3. Submit for review।

### ধাপ ৪: রিভিউ

Figma-র লোক হাতে দেখে। কত দিন লাগবে তার নির্দিষ্ট সময় ওরা বলে না — কাজের চাপের
উপর নির্ভর করে, সাধারণত কয়েক দিন। ফল আপনার Figma অ্যাকাউন্টের ইমেইলে আসবে।

### networkAccess নিয়ে রিভিউয়ার কী দেখবে

আমাদের manifest-এ আছে:

```json
"networkAccess": {
  "allowedDomains": ["ws://localhost:7878"],
  "reasoning": "LazyLord talks only to the local LazyLord bridge ..."
}
```

localhost ব্যবহার করলে Figma `reasoning` লেখা বাধ্যতামূলক করে, সেটা আছে। কথাটা
সত্যি — প্লাগিন শুধু নিজের মেশিনের প্যানেলে কথা বলে, বাইরে কিছু যায় না।
রিভিউয়ার প্রশ্ন করলে এটাই বলবেন, আর বলবেন Adobe প্যানেল ছাড়া প্লাগিন নিজে
কিছুই পাঠায় না।

### দুটো কথা

- **FigJam**: manifest-এ `"editorType": ["figma", "figjam"]` আছে। FigJam-এ
  ভালো করে পরীক্ষা না করে থাকলে শুধু `["figma"]` রেখে দিন — যেটা কাজ করে না
  সেটা তালিকায় থাকলে রিভিউয়ে ধরা পড়ে।
- **প্রাইভেট রাখা**: শুধু নিজের টিমের জন্য হলে Community-তে না দিয়ে
  organization-এ প্রাইভেট প্রকাশ করার সুযোগ আছে।

---

## ৪. ছাড়ার আগে চেকলিস্ট

- [ ] `TESTING.md`-এর ⚠ ঘরগুলো আসল অ্যাপে চালানো হয়েছে, বিশেষ করে
      Photoshop-এর XMP আর AE/AI-র ট্যাগ সেভ-রিওপেনে টেকে কিনা।
- [ ] সব টেস্ট পাস: `npm run test:core`, `npm run test:bridge`, আর
      `cscript //Nologo tools\test-*.js`।
- [ ] `package.json`-এ ভার্সন বাড়ানো হয়েছে।
- [ ] `npm run release` চলেছে, আর "Signature verified successfully" এসেছে।
- [ ] কমিটে বা ফাইলে নিজের নাম ছাড়া অন্য কারও নাম নেই।
- [ ] বানানো জিপটা নিজে একবার খুলে `Install LazyLord.bat` চালিয়ে দেখা হয়েছে,
      আর তিনটে অ্যাপেই প্যানেল খুলেছে।
      (এটা ডেভেলপার ইনস্টলের জায়গাটাই দখল করে নেয়; আগের অবস্থায় ফিরতে
      `install.bat` আবার চালালেই হবে।)
- [ ] README-তে ভার্সন আর ডাউনলোড লিংক ঠিক আছে।
- [ ] সাইনিং কি ব্যাকআপ করা আছে।
