{
  "name": "@lazylord/core",
  "version": "1.1.2",
  "description": "Shared intermediate representation, transfer protocol and SVG path math for LazyLord.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "commonjs",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
